name: Deploy Vote Indexer (Production)

on:
  push:
    branches:
      - main
    paths:
      - 'scoring-service/vote-indexer/**'
      - 'scoring-service/deployment/production/vote-indexer.yaml'
      - 'hermes-schema/**'
      - 'wire/**'
      - '.github/workflows/scoring-vote-indexer-deploy.yml'

concurrency:
  group: vote-indexer-production
  cancel-in-progress: true

env:
  REGISTRY: registry.digitalocean.com/geo
  IMAGE_NAME: vote-indexer

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DigitalOcean Container Registry
        run: doctl registry login --expiry-seconds 1200

      - name: Build and push Docker image
        run: |
          docker build -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} \
                       -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest \
                       -f scoring-service/vote-indexer/Dockerfile .
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubectl for DigitalOcean
        run: |
          doctl kubernetes cluster kubeconfig save ${{ secrets.DIGITALOCEAN_CLUSTER_NAME }}

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f scoring-service/deployment/production/namespace.yaml
          # Update image tag to use SHA and apply
          sed -i 's|image: registry.digitalocean.com/geo/vote-indexer:latest|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}|g' scoring-service/deployment/production/vote-indexer.yaml
          kubectl apply -f scoring-service/deployment/production/vote-indexer.yaml

      - name: Restart deployment to pick up new image
        run: |
          kubectl rollout restart deployment/vote-indexer -n scoring

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/vote-indexer -n scoring --timeout=120s

      - name: Show deployment status
        run: |
          echo "=== Vote Indexer Deployment ==="
          kubectl get deployment vote-indexer -n scoring
          echo ""
          echo "=== Vote Indexer Pods ==="
          kubectl get pods -n scoring -l app=vote-indexer
          echo ""
          echo "=== Recent Events ==="
          kubectl get events -n scoring --sort-by='.lastTimestamp' | grep vote-indexer | tail -10
