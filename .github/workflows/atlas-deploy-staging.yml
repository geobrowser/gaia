name: Deploy Atlas (Staging)

on:
  push:
    branches:
      - dev
    paths:
      - 'atlas/**'
      - 'hermes-instrumentation/**'
      - 'hermes-relay/**'
      - 'hermes-schema/**'
      - 'hermes-substream/**'
      - 'stream/**'
      - 'wire/**'
      - 'hermes/k8s/staging/atlas.yaml'
      - '.github/workflows/atlas-deploy-staging.yml'

concurrency:
  group: atlas-staging
  cancel-in-progress: true

env:
  REGISTRY: registry.digitalocean.com/geo
  IMAGE_NAME: atlas

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DigitalOcean Container Registry
        run: doctl registry login --expiry-seconds 1200

      - name: Build and push Docker image
        run: |
          docker build -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} \
                       -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging \
                       -f atlas/Dockerfile .
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Configure kubectl for DigitalOcean
        run: |
          doctl kubernetes cluster kubeconfig save ${{ secrets.DIGITALOCEAN_CLUSTER_NAME }}

      - name: Delete existing job (jobs are immutable)
        run: |
          kubectl delete job atlas -n kafka-staging --ignore-not-found=true

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f hermes/k8s/staging/namespace.yaml
          # Update image tag to use SHA and apply
          sed -i 's|image: registry.digitalocean.com/geo/atlas:latest|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}|g' hermes/k8s/staging/atlas.yaml
          kubectl apply -f hermes/k8s/staging/atlas.yaml

      - name: Wait for job to start
        run: |
          kubectl wait --for=condition=ready pod -l app=atlas -n kafka-staging --timeout=60s || true

      - name: Show deployment status
        run: |
          echo "=== Atlas Job ==="
          kubectl get job atlas -n kafka-staging || echo "Job not found"
          echo ""
          echo "=== Atlas Pods ==="
          kubectl get pods -n kafka-staging -l app=atlas
          echo ""
          echo "=== Recent Events ==="
          kubectl get events -n kafka-staging --sort-by='.lastTimestamp' | tail -10
