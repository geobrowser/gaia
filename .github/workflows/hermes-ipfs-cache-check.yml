name: Hermes IPFS Cache Check

on:
  push:
    branches: [main, develop]
    paths:
      - 'hermes-ipfs-cache/**'
      - 'hermes-relay/**'
      - 'hermes-substream/**'
      - 'hermes-kafka/**'
      - 'ipfs/**'
      - 'stream/**'
  pull_request:
    branches: [main, develop]
    paths:
      - 'hermes-ipfs-cache/**'
      - 'hermes-relay/**'
      - 'hermes-substream/**'
      - 'hermes-kafka/**'
      - 'ipfs/**'
      - 'stream/**'

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Build & Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./hermes-ipfs-cache

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.92.0"
          components: rustfmt, clippy

      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Install build dependencies
        run: |
          sudo apt-get install -y \
            cmake \
            libclang-dev \
            libssl-dev \
            pkg-config

      - name: Cache Dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ./hermes-ipfs-cache

      - name: Check formatting
        run: cargo fmt --check

      - name: Run clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Build
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --verbose
