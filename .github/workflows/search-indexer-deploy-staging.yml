name: Deploy Search Indexer (Staging)

on:
  push:
    branches:
      - dev
    paths:
      - 'search-indexer/**'
      - 'search-indexer-shared/**'
      - 'search-indexer-repository/**'
      - 'search-indexer-deploy/k8s/staging/**'
      - 'sdk/**'
      - 'hermes-schema/**'
      - 'wire/**'
      - 'indexer_utils/**'
      - '.github/workflows/search-indexer-deploy-staging.yml'

concurrency:
  group: search-indexer-staging
  cancel-in-progress: true

env:
  REGISTRY: registry.digitalocean.com/geo
  IMAGE_NAME: search-indexer

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Install doctl
        uses: digitalocean/action-doctl@3cb3953159719656269e044e0e24ca16dd2a690f # v2.5.2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DigitalOcean Container Registry
        run: doctl registry login --expiry-seconds 1200

      - name: Build and push Docker image
        run: |
          docker build -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} \
                       -t ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging \
                       -f search-indexer/Dockerfile .
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:staging

      - name: Set up kubectl
        uses: azure/setup-kubectl@v5

      - name: Configure kubectl for DigitalOcean
        run: |
          doctl kubernetes cluster kubeconfig save ${{ secrets.DIGITALOCEAN_CLUSTER_NAME }}

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f search-indexer-deploy/k8s/staging/namespace.yaml
          # Remove legacy Deployment if it exists (one-time migration to StatefulSet)
          kubectl delete deployment search-indexer -n search-staging --ignore-not-found
          # Update image tag to use SHA and apply
          sed -i 's|image: registry.digitalocean.com/geo/search-indexer:latest|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}|g' search-indexer-deploy/k8s/staging/search-indexer.yaml
          kubectl apply -f search-indexer-deploy/k8s/staging/search-indexer.yaml

      - name: Wait for rollout to be ready
        run: |
          kubectl rollout status statefulset/search-indexer -n search-staging --timeout=120s

      - name: Show deployment status
        run: |
          echo "=== Search Indexer StatefulSet ==="
          kubectl get statefulset search-indexer -n search-staging || echo "StatefulSet not found"
          echo ""
          echo "=== Search Indexer Pods ==="
          kubectl get pods -n search-staging -l app=search-indexer
          echo ""
          echo "=== Recent Events ==="
          kubectl get events -n search-staging --sort-by='.lastTimestamp' | tail -10
