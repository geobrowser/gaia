name: Hermes Pipeline Check

on:
  push:
    branches: [main, dev]
    paths:
      - 'hermes-pipeline/**'
      - 'hermes-instrumentation/**'
      - 'hermes-relay/**'
      - 'hermes-schema/**'
      - 'hermes-substream/**'
      - 'hermes-kafka/**'
      - 'stream/**'
      - 'wire/**'
  pull_request:
    branches: [main, dev]
    paths:
      - 'hermes-pipeline/**'
      - 'hermes-instrumentation/**'
      - 'hermes-relay/**'
      - 'hermes-schema/**'
      - 'hermes-substream/**'
      - 'hermes-kafka/**'
      - 'stream/**'
      - 'wire/**'

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Build & Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./hermes-pipeline

    steps:
      - uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.92.0"
          components: rustfmt, clippy

      - name: Install protoc
        run: sudo apt-get update && sudo apt-get install -y protobuf-compiler

      - name: Install build dependencies
        run: |
          sudo apt-get install -y \
            cmake \
            libclang-dev \
            libcurl4-openssl-dev \
            libssl-dev \
            pkg-config

      - name: Cache Dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ./hermes-pipeline

      - name: Check formatting
        run: cargo fmt --check

      - name: Run clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Build
        run: cargo build --verbose

      - name: Run tests
        run: cargo test --verbose
